services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Том на каталог: volume нельзя надёжно монтировать в файл db.sqlite3
      - db_data:/app/data
    env_file:
      - .env
    environment:
      DJANGO_SETTINGS_MODULE: myproject.settings

volumes:
  db_data:
